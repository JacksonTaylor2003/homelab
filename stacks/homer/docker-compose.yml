services:
  homer:
    image: b4bz/homer:latest
    container_name: homer
    env_file:
      - /opt/homelab/homer/.env
    volumes:
      - ${CONFIG_PATH}/assets:/www/assets
    ports:
      - "8085:8080"
    restart: unless-stopped